{
  "name": "other web",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm vite",
    "build:2025": "cd haji5 && pnpm install && pnpm build && mv dist ../dist/haji5",
    "build": "mkdir dist && pnpm build:2025 && cp static/* dist/",
    "preview": "pnpm vite preview"
  }
}
